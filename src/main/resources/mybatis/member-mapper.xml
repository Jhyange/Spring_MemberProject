<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberDAO">

<update id="update">
update member set pw=#{pw},name=#{name}, phone=#{phone}, phone1=#{phone1}, phone2=#{phone2} 
, email=#{email} , post=#{post} , address1=#{address1} , address2=#{address2} where id=#{id}
</update>


</mapper>