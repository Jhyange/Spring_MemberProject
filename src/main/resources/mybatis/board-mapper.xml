<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardDAO">


<select id="selectlist" resultType="kh.spring.dto.BoardDTO"> select * from board</select>
<insert id="insert">
insert into board values(b_seq.nextval,#{title},#{writer},#{context},default,0,#{imgpath})
</insert>
<select id="selectcontext" resultType="kh.spring.dto.BoardDTO"> select * from board where seq=#{seq}</select>
<delete id="deletecontext">delete from board where seq=#{seq}</delete>
</mapper>